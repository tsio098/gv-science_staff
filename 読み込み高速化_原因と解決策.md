# 先生用ダッシュボード 読み込み高速化 — 原因と解決策まとめ

作成日: 2026/06/07

## 概要

「URL を開く → パスワード(great098)入力 → 生徒一覧が表示されるまで」の時間を短縮する取り組みの記録。
当初コールドで 30〜50 秒、本セッション開始時点でもパスワード入力後 14〜34 秒かかっていたものを、**最終的に約 1.6 秒（即表示）+ 裏で最新化**まで短縮した。

構成: バックエンド = GAS Web App（`gas/Code.gs`）／フロント = GitHub Pages（`docs/`、`tsio098/gv-science_staff`）。
スナップショット方式: 30 分ごとの時間トリガー `rebuildSnapshot` が一覧＋全詳細を再計算してキャッシュに格納し、利用者はそこから即返す設計。

## 計測の前提（信頼できる指標）

- **GAS 実行ログの実行時間**（サーバ側の実測）と、**フロントの fetch promise 実測（appFetchMs）** を信頼する。
- ブラウザのバックグラウンドタブは `setInterval` がスロットルされ「描画検知時間」が過大に出るため使わない。
- 連続リロード＋並行 fetch は GAS 側に再計算を滞留させ計測を乱すので避ける。

---

## 原因と解決策（発見順）

### 1. 欠席率シートの I/O（per-tab 読み）

- **症状**: 30 分トリガーの再計算が遅い。内訳で欠席率(absence)が最大 14〜21 秒。
- **原因**: (学年×科目) 10 タブ（既卒×5＋H3×5）を 1 タブずつ読み、1 タブあたり `getLastRow`＋B列＋E列＝3 往復。データ量ではなく **GAS の API 往復オーバーヘッド**が壁（10 タブ×約1.4秒）。
- **解決策**: 全タブの B列・E列（=20 レンジ）を **Sheets API `Values.batchGet` で 1 回の HTTP に集約**。`Sheets` 未有効/例外時は従来の per-tab 読みへ自動フォールバック。
- **効果**: absence ≈ 14.6 秒 → **約 0.8〜2.5 秒**（`ABS batchGet: tabs=10, 330〜597ms`）。
- **前提**: GAS エディタの「サービス」で Google Sheets API（識別子 Sheets, v4）を有効化＋`appsscript.json` に高度なサービス依存を追加。

### 2. 演習成績シートの I/O（per-sheet 読み）

- **症状**: absence 解決後、再計算の新たな最大は scores（演習成績）で **13.3 秒**。
- **原因**: 5 科目シート（化学/生物/化学基礎/生物基礎/地学基礎）を 1 枚ずつ、`openById`＋`getLastRow`＋`getLastColumn`＋`getValues`＋`getNumberFormats` ≈ 1 枚 4〜5 往復 × 5 枚 ≈ 25 往復。これも往復回数の壁。
- **解決策（案1a＋案3）**:
  - 5 科目は同一スプレッドシート内なので、**2 回の `Values.batchGet`** で全シートを取得（①`UNFORMATTED_VALUE`＋日付は`FORMATTED_STRING`＝完全精度の数値・日付文字列、②`FORMATTED_VALUE`＝`%` 列検出で `getNumberFormats` の代替）。`readNum_` 互換の書式情報を再構成し、現挙動（%列の×100、偏差値の精度）を完全再現。
  - `getSheet_` の **`openById` をメモ化**（同一ブックを実行中 1 回だけ開く。roster/mock にも波及）。
- **効果**: scores 13,333ms → **約 1,177〜1,338ms（約 11 倍）**。トリガー全体 24 秒 → 約 11.6〜13.4 秒。

### 3. CacheService（L1 キャッシュ）の揮発

- **症状**: 「キャッシュヒットは 0.5〜0.7 秒」なのに、しばらくぶり/初回アクセスは十数秒。温め直した直後でも数分後にはミスする。
- **原因**: スナップショット（一覧 20KB＋詳細チャンク＝計 約150KB／152 キー）を `CacheService` に書くと、TTL（6時間設定）内でも **実際には数分で揮発**する。`students` キーが追い出され、毎回ミス→再計算に落ちていた。
- **解決策（L2 永続化）**: 一覧 JSON を **gzip 圧縮して `ScriptProperties` に保存**（揮発しない）。`getStudents_` の読み順を **L1(CacheService) → L2(ScriptProperties) → 軽量再計算** に変更。1 プロパティ 9KB 上限に対し base64 をチャンク分割、保存時刻で鮮度検証、整合性チェック、空結果では保存しない、失敗時は従来再計算へフォールバック。
- **効果**: 圧縮後 約 4KB（1 チャンク）。非 fresh の `/exec` 応答は **約 1.8 秒**（L1/L2 ヒット）に安定。

### 4. ★真因：フロントがログイン時に「fresh=1（全再計算）」を強制していた

- **症状**: GAS をいくら速くしても、パスワード入力後は毎回 14〜34 秒のまま。
- **原因**: フロント `docs/app.jsx` のログイン成功ハンドラで `freshRef.current = true` をセットしており、ログイン直後の取得が `?fresh=1` 付き。サーバは `getStudents_(fresh=true) → rebuildAll_(true)`＝**全再計算を強制**し、L1/L2 キャッシュを毎回バイパスしていた。
  - 検証: アプリのログイン fetch は `fresh=1` 付きで 27 秒。一方 `fresh` 無しの直叩きは 1.8 秒。これまで「キャッシュミス」と見ていた十数秒は、実はすべてこの強制フル再計算だった。
- **解決策（即表示＋裏で最新化）**:
  - ログイン時の `freshRef.current = true` を**削除** → ログインは**非 fresh＝即表示**（L1/L2 から約 1.6 秒）。
  - 一覧取得後、**裏で fresh を 1 回取得して反映**（SWR）。≤30 分鮮度の一覧を即出ししつつ、最新入力も少し遅れて自動反映。
  - `dist/app.js` を再ビルド、`index.html` の `?v=` を更新（キャッシュ無効化）してデプロイ（git push → GitHub Pages）。
- **効果**: パスワード→一覧表示 **14〜34 秒 → 約 1.6 秒**。裏の fresh（約 16 秒・全再計算）は非ブロッキングで走り、完了時に一覧が静かに差し替わる。

---

## 最終結果

| 指標 | Before | After |
|---|---|---|
| パスワード→一覧表示 | 14〜34 秒 | **約 1.6 秒** |
| ログイン初回 fetch | `fresh=1`（全再計算・ブロッキング） | `fresh=false`（キャッシュ/L2・非ブロッキング） |
| 欠席率(absence) 取得 | 14〜21 秒（per-tab） | 約 0.3〜2.5 秒（batchGet） |
| 演習成績(scores) 取得 | 13.3 秒（per-sheet） | 約 1.2 秒（batchGet） |
| キャッシュミス時の一覧 | 十数秒（再計算） | 約 1.8 秒（L2 から）|

体感の決定打は **原因4（フロントの強制 fresh 解除）**。GAS 側の最適化（1〜3）は「裏の再計算を速く保つ」「L1 揮発時も非 fresh が速いまま」を担保する土台として効いている。

## 残課題・補足

- **裏の fresh（全再計算 約16秒）が毎ログイン 1 回走る**ためサーバ負荷はやや高め。同時ログインが多いと再計算が直列化して待つ可能性。必要なら「前回 fresh から N 分以上経過時のみ裏更新」の間引きを追加できる。
- **rebuild 自体の残コスト**: roster（生徒IDシート、0.4〜11 秒と変動大）と mock（マーク模試、約 9 秒）。利用者の表示には影響しなくなったが、将来 `Values.batchGet` 化でさらに短縮余地。
- **詳細ページ（生徒個別）** は L2 永続化の対象外（CacheService 依存）。コールド時は再計算が残る。必要なら詳細も永続化可能。
- **運用上の区別**: トリガー=Head（保存コード）／Web アプリ=デプロイ版。トリガー検証は保存だけでよいが、利用者反映にはデプロイが必要。エディタ Code.gs とデプロイ版の一致は SHA-256 で確認できる。

## 関連ファイル

- `gas/Code.gs` … batchGet 化（scores/absence）、openById メモ化、L2 永続化（`saveStudentsProp_`/`loadStudentsProp_`）、`getStudents_` の L1→L2→再計算
- `gas/appsscript.json` … 高度なサービス Sheets v4、oauthScopes（spreadsheets / script.scriptapp）
- `docs/app.jsx` … ログインの強制 fresh 解除＋「即表示＋裏で最新化」
- `docs/index.html` … `?v=` キャッシュ無効化バージョン

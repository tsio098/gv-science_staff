# 先生用ダッシュボード 読み込み高速化：原因と対策まとめ

対象：GASバックエンド `gas/Code.gs`（スナップショット＋キャッシュ方式）
ゴール：「URL→パスワード入力→生徒一覧表示」までの時間短縮（当初コールドで30〜50秒）

---

## 全体像

利用者の**通常アクセス（キャッシュヒット）は当初33.9秒 → 0.5〜0.8秒**に改善。
残る遅さは「**キャッシュミス時**」と「**30分トリガーの全再計算**」の2つで、いずれも真因は**スプレッドシートI/O（特に欠席率シート）**だった。

---

## 1. キャッシュミス時に「全再計算」へ落ちていた

**原因**
`getStudents_` はキャッシュ未ヒット時に `rebuildAll_`（全再計算）を**同期実行**していた。`rebuildAll_` の重さは主に「マーク模試2.9万行の読込」と「全47名の詳細構築」で、これがユーザーの一覧アクセスに巻き込まれ30〜46秒かかっていた。
一覧表示（`buildStudentsList_`）はマーク模試も詳細も使わないのに、それらを読んでいた。

**対策**
`getStudents_` を分岐。`fresh=1`（更新ボタン）のみ従来の全再計算。**非freshのミス時は新設 `rebuildStudentsList_()`（一覧だけ＝マーク模試も全詳細も読まない軽量版）**を呼ぶように変更。専用の鮮度キー `students_epoch` を導入し、全再計算の二重防止ロジックと干渉させない。

**結果**：ミス時でも30〜46秒の全再計算に落ちなくなった（ただし下記4・5の理由で軽量版もまだ遅かった）。

---

## 2. `setupSnapshotTrigger` が権限エラー

**原因**
`appsscript.json` の `oauthScopes` に `spreadsheets` しかなく、`ScriptApp.getProjectTriggers()`（トリガー操作）に必要な `script.scriptapp` が無かった。
→ `Specified permissions are not sufficient to call ScriptApp.getProjectTriggers` エラー。

**対策**
`oauthScopes` に `https://www.googleapis.com/auth/script.scriptapp` を追加し、再承認。

**結果**：30分トリガーが作成でき、キャッシュが温まり続けるようになった（＝通常アクセス0.6秒の前提）。

---

## 3. マーク模試2.9万行を毎回フル読み

**原因**
`loadMockByName_` がマーク模試タブ（約29,000行）を毎回全行読んでいた。詳細に必要なのは今年度分だけ。

**対策**
今年度（4/1）以降の行だけ読む `MOCK.LIMIT_TO_FISCAL_YEAR`。A列タイムスタンプ（昇順）から今年度開始行を特定し、そこから末尾だけ読む。
※年度開始は `Date.UTC(年,3,1)-9h`（JST固定、1〜3月は前年度4/1）。

**結果**：今年度の対象は約83行に激減。ただし「先頭行を探すためのA列スキャン」は残り、読込は約6〜8秒（下記6も参照）。

---

## 4. 真因は「詳細構築」でも「キャッシュ書込」でもなく、シートI/Oだった

**原因（プロファイリングで判明）**
`rebuildAll_` に各処理の所要時間ログを仕込んだところ、内訳は：

| 処理 | 所要 | 備考 |
|---|---|---|
| roster（生徒IDシート） | 約1.5〜11秒 | openByアイディーのオーバーヘッド |
| scores（化学/生物 成績） | 約2.5〜7.6秒 | 同上 |
| mock（今年度83行） | 約6〜8秒 | 2.9万行のA列スキャン分 |
| **absence（欠席率）** | **約14〜26秒（最大）** | 後述 |
| build（詳細構築） | 約0.1秒 | **問題なし** |
| cacheWrite（キャッシュ書込） | 約0.3秒 | **問題なし** |

当初「詳細構築やキャッシュ書込が重い」と推測したが**全くの誤り**で、ボトルネックは**シート読み込みのAPI往復オーバーヘッド**だった。

**対策の方向**：読むデータ量と「API呼び出し回数」を減らす（下記5〜7）。

---

## 5. 欠席率が最大ボトルネック（約14〜21秒）

**原因（段階的に判明）**
- `buildAbsenceMapsForRoster_` が学年×科目の**10タブ（既卒×5＋H3×5）**を読んでいた。
- 欠席率列(E)は**日次出席グリッドの右側**にあるため、全列読み／スパン読みでは**グリッドを丸ごと読んで**しまい遅い。
- B列・E列だけを別々に読んでも、**1タブあたり3往復（getLastRow＋名前列＋欠席率列）×10タブ＝約14秒**。これはデータ量ではなく**GASのAPI往復レイテンシ**が壁だった（1タブ約1.4秒）。

**対策（積み上げ）**
1. シート名→Sheetマップを1回だけ作る `absSheetMap_()`（`getSheets()` の呼び直しを排除）。
2. 全列読みをやめ、**生徒名列(B)・欠席率列(E)だけ**読む。
3. レイアウトを固定設定化（`ABSENCE.FIXED=true / NAME_COL=2(B) / RATE_COL=5(E) / DATA_START_ROW=4`、全タブ共通）し、**ヘッダー自動検出（先頭12行×全列の読み）を完全スキップ**。
4. **本命：Google Sheets API の `batchGet`** で、全10タブのB列・E列＝20レンジを**1回のHTTP**で一括取得（`buildAbsenceMapsForRoster_` を全面改修、`colLetter_` 追加）。`Sheets` 未有効時は従来読みにフォールバック。`appsscript.json` に高度なサービス `Sheets(v4)` を追加。

**結果（見込み）**：欠席率 約14秒 → **約2秒**。トリガー全体 約32秒 → **約12秒**見込み。
※ **この `batchGet` は実装済みだが本番検証が未完**（次セッションで「保存→Sheets API有効化→`rebuildSnapshot`実行→ログ確認→再デプロイ」を行う）。

---

## 6. マーク模試の「末尾スライス読み」はバグだった

**原因**
高速化のため「末尾2000行だけ読む」方式を試したが、実シート末尾に**空行（書式だけの行）**があり `getLastRow()` が実データより下を指すため、末尾スライスが空を読み**今年度データを取りこぼし（rows=0、詳細のマーク模試が消える）**。

**対策**
末尾スライスをやめ、**A列タイムスタンプ1列を全走査する堅牢版**に戻した（空行があっても取りこぼさない）。読込は約6〜8秒で許容。

**教訓**：フォーム回答シートは末尾に空行が混じり得るので、`getLastRow()` 基準の末尾アクセスは要注意。

---

## 7. ブラウザでの計測そのものの落とし穴

**原因**
- 計測タブがバックグラウンドだと `setInterval` がスロットルされ、「描画検知時間」が実際より大きく出る（fetch完了後でも100秒等の誤計測）。
- 連続リロード＋並行fetchはGAS側に再計算を滞留させ、計測値を乱す。
- GASの `web_fetch` は302リダイレクトで本文が空になる。

**対策**
- 信頼するのは `fetch` promise実測の `appFetchMs`。
- エンドポイント確認は Claude in Chrome のページ内 `fetch` で。
- パスワードゲートを再表示して測るには `sessionStorage.clear()`＋reload。
- **権威ある一次情報はGASの「実行数」ログ**（doGetの期間、rebuildSnapshotの所要、PROFILE行）。

---

## 8. デプロイの落とし穴（反映されない問題）

**原因**
- **トリガー（`rebuildSnapshot`）は Head＝エディタで保存したコード**で動く。
- **Webアプリ（`doGet`）はデプロイ済みバージョン**で動く。
- 保存しただけでデプロイしていないと、トリガーには効くが利用者（Webアプリ）には反映されない。逆もしかり。
- あるとき新ログが出なかったのは、**保存前の状態で実行**していたため（コード自体は正しく貼られていた）。

**対策**
- トリガー検証＝**保存**だけでよい。
- 利用者へ反映＝**新バージョンをデプロイ**（/exec URLは変えない）。
- 反映確認は「実行数」ログの PROFILE 行で。

---

## 残る最大の改善余地（未着手）

**一覧データの永続化（ScriptProperties＋gzip）**
batchGetで欠席率を速くしても、キャッシュが揮発した瞬間のユーザーは `rebuildStudentsList_`（roster＋scores＋absence）で依然十数秒かかる。
**20KBの一覧JSONを揮発しないストア（ScriptProperties、gzip圧縮で9KB制限内）に永続化**すれば、キャッシュが切れても**約0.1秒で返せて再計算ゼロ**＝コールド/再デプロイ直後でも一覧が常に速くなる。これがコールド時の本丸。

---

## まとめ（一言で）

> 「重いのは計算ではなくシート読み込みのAPI回数」。
> ユーザー向けは"重い処理を踏ませない分離＋温キャッシュ"で0.6秒に到達。
> 残りは"バックグラウンドのトリガーを batchGet で軽くする"＋"一覧を永続化してミスを無くす"の2点。

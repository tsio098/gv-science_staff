# GV Science 先生用「生徒成績一覧」アプリ — UI/UX 設計ブリーフ

このドキュメントは、Claude Design に UI/UX を作成させるための指示書です。既存の生徒向け LIFF アプリ「GV Science」成績推移画面の仕組みを土台に、**先生が生徒全員の成績推移を閲覧できる Web アプリ**を新規に設計します。デザイナーは本書だけで画面・コンポーネント・状態・インタラクションを設計できることを目標とします。

---

## 1. このアプリの目的

先生が、担当する生徒全員の演習テスト成績推移をひとつの Web アプリで把握できるようにする。中心となる体験は **「生徒一覧」から生徒を選び、その生徒の成績推移を個人詳細画面で深掘りする** という流れ。個人詳細は、既存の生徒向け成績推移画面とほぼ同じ見え方を踏襲する（生徒・先生で同じ情報を見られるようにする）。

対応科目は **化学・生物・化学基礎・生物基礎・地学基礎** の 5 つ。生徒ごとに履修科目が異なる。

---

## 2. 利用者・利用環境

- **利用者**: 塾・予備校の先生（複数生徒を担当）。データ閲覧が主目的で、編集はしない（読み取り専用ビュー）。
- **デバイス**: PC（デスクトップ）が主。一覧で多くの生徒を俯瞰するため横幅を活かしたレイアウトを基本とする。ただしタブレット・スマホでも崩れない**レスポンシブ**対応を必須とする。
- **既存の生徒アプリ**は LINE 内 LIFF（縦長スマホ）だったが、本アプリは独立した Web アプリでよい。

---

## 3. データ構造（設計の前提）

バックエンド（GAS）は 1 回のリクエストで 1 生徒分の全履修科目をまとめて返す。先生用では「生徒の一覧」と「特定生徒の詳細」の 2 種類のデータを扱う想定で設計する。

### 3-1. 生徒1人分の成績データ（個人詳細で使う）

```
{
  name: string,                 // 生徒名
  subjects: string[],           // 成績データが実在する履修科目だけを表示順で並べた配列（科目キー）
  data: {
    [科目キー]: {
      fields: string[],         // その科目の分野名一覧（本人が一度も受けていない分野は除外済み）
      months: string[],         // 月ラベル一覧（"yyyy-MM"）
      totalTrend: [             // テストごとの合計点推移（実施日昇順）
        { date, testName, total, average, hensachi }
      ],
      rate:     { [分野名]: (number|null)[] },  // 分野ごと → months と同じ長さの配列（各月の得点率、欠測は null）
      avgRate:  { [分野名]: (number|null)[] },  // 同上（クラス平均得点率）
      hensachi: { [分野名]: (number|null)[] }   // 同上（偏差値）
    }
  }
}
```

- `rate` / `avgRate` / `hensachi` は「分野名 → 月数と同じ長さの配列」で、各要素が各月の値。**欠測月は `null`**。
- 値のスケール：得点率・平均得点率は 0〜100、偏差値は概ね 25〜75 程度。

### 3-2. 生徒一覧用データ（先生用で新たに必要）

一覧表示では全生徒の要約指標が必要。各生徒について、科目ごとに「直近テストの合計点・偏差値・前回比」「直近月の平均得点率」程度のサマリーを持つ想定でデザインする（実データのキー名はデザインに影響しないため、必要な項目を仮に置いてよい）。最低限、次を一覧で見せられること：

- 生徒名 / 履修科目
- 科目ごとの **直近偏差値** と **前回比（↑↓と差分）**
- 科目ごとの **直近合計点**
- 最終受験日（データの新しさ）

### 3-3. エラー・空状態

- `NEEDS_PAIRING`：未ペアリング（生徒側の概念。一覧では「未連携」バッジ等で表現）
- `NO_DATA`：成績ゼロ件（「データなし」表示）

---

## 3-bis. システム構成（バックエンド GAS / フロントエンド GitHub Pages）

本アプリは **バックエンド = Google Apps Script（GAS）Web App**、**フロントエンド = GitHub Pages にホストする静的サイト** の構成で実装する。デザイン上も、この構成に起因する制約（認証方式・通信の仕方・状態）を前提に画面を設計する。

### 構成図（概念）

```
[先生のブラウザ]
   └─ GitHub Pages（静的フロント：HTML/JS/CSS）
        └─ fetch(GASのWebApp URL?action=...&token=...)  ← GET
             └─ GAS Web App（doGet）
                  ├─ トークン照合（Script Property）
                  ├─ 「演習点数報告」スプレッドシート読み取り
                  └─ JSON を ContentService で返却
```

### フロントエンド実装方針（おまかせ → 推奨：React + Vite）

- 既存の生徒アプリが React のため、部品の考え方を流用しやすい **React（Vite ビルド）** を推奨。ビルド成果物（静的ファイル）を GitHub Pages にデプロイする。
- ルーティングは GitHub Pages の都合上 **ハッシュルーティング（`#/students`, `#/student/:name`）** を使う（サブパス配信でも 404 にならないため）。
- GAS の Web App URL とトークンは**ビルド時の環境変数 or 設定ファイル**で注入する想定（フロントにベタ書きしない運用を推奨）。
- グラフは既存方針どおり **外部チャートライブラリを使わず手書き SVG**。

### 認証方式（決定：秘密の URL / トークン）

- 先生には**推測困難なトークン付き URL**を配布する（例：`https://<org>.github.io/gv-science-teacher/#/students?token=XXXX`、または初回入力後に保持）。
- フロントは全ての GAS リクエストに `token` を付与。GAS 側は **Script Property に保存した正規トークンと照合**し、不一致なら `UNAUTHORIZED` を返す。
- トークンはフロントの **`sessionStorage`** に保持し、タブを閉じると消える（共有 PC での残留を避ける）。URL から読み取ったら URL からは消す（履歴汚染防止）。
- 注意点としてデザインに反映すること：
  - **「トークン入力 / 認証エラー」画面**が必要（URL にトークンが無い・無効なとき）。
  - これは簡易認証であり、トークンが漏れると誰でも閲覧できる旨を運用上の前提とする（生徒個人情報を扱うため、トークンは限定配布・定期ローテーション推奨。設計ブリーフ上の注記に留める）。

### 通信の制約（CORS）

- GAS Web App への **GET（ContentService で JSON 返却）** はクロスオリジンで取得可能。本アプリは**読み取りのみ**なので原則 GET で完結させる設計にする。
- プリフライトが発生する POST/カスタムヘッダーは避ける（CORS で詰まりやすいため）。認証トークンは**ヘッダーではなくクエリパラメータ**で渡す。
- 通信失敗・429/5xx に備え、フロントは**指数バックオフ付きリトライ**を行う（既存仕様踏襲）。

### バックエンド API（GAS エンドポイント）

既存の `?action=scores` は「LINE ユーザー ID → 生徒」を前提にしていたが、先生用では**生徒名指定**で取得できるようにする。おまかせ回答のため、以下を**推奨設計**として提示する（実装側で調整可）。

| action | 用途 | 主なパラメータ | 返却 |
|---|---|---|---|
| `students` | 生徒一覧（要約） | `token` | 全生徒の要約配列（名前・履修科目・科目ごとの直近偏差値/合計点/前回比・最終受験日） |
| `scores` | 生徒個人詳細 | `token`, `name`（生徒名） | 第3章の「生徒1人分の成績データ」JSON |

- `students` は一覧描画に必要な**軽量な要約だけ**を返す新規エンドポイント（全生徒分の重い詳細を一度に返さない）。一覧の高速表示とサーバ負荷軽減のため。
- 個人詳細は従来どおり **1 生徒1リクエストで全履修科目をまとめて返す**（タブ切替で再通信しない）。
- キャッシュ：`students` は `CacheService`（スクリプトキャッシュ）に短時間、`scores` は生徒ごとにキャッシュ。フロントは更新ボタンで `fresh=1` を付けて取り直す（既存踏襲）。
- エラーは HTTP 200 のまま body に `error` を入れる方式（`UNAUTHORIZED` / `NO_DATA` など）。

### デプロイ / 運用メモ（実装者向け）

- フロント：`main` ブランチ push → GitHub Actions（または `gh-pages`）で Pages 公開。
- バックエンド：GAS を「ウェブアプリ」として新規デプロイ。実行ユーザーは「自分」、アクセスは「全員」。URL が変わるとフロント設定の更新が必要。
- GAS 実行ユーザーが「演習点数報告」スプレッドシートを読める共有設定が必要（既存前提）。

---

## 4. 画面構成（全体）

3 つの主要画面で構成する。

1. **トークン認証 / 認証エラー**（秘密 URL 方式。下記参照）
2. **生徒一覧（ホーム）** ← 起点
3. **生徒個人詳細（成績推移）** ← 既存画面を踏襲

加えて、共通のヘッダー（アプリ名・更新ボタン・検索）を持つ。

### 画面⓪：トークン認証 / 認証エラー

- URL に有効なトークンがあれば**素通り**で生徒一覧へ（このときログイン画面は一瞬も出さなくてよい）。
- トークンが無い / 無効（`UNAUTHORIZED`）のとき：トークン入力欄＋「アクセスできない場合は管理者に連絡」程度の最小画面。
- 認証中はスケルトン/スピナー。

---

## 5. 画面①：生徒一覧（ホーム）

先生が最初に開く画面。全生徒を俯瞰し、気になる生徒へ素早く到達できることが最重要。

### レイアウト

- 上部に**検索バー**（生徒名で絞り込み）と**科目フィルタ**（化学/生物/化学基礎/生物基礎/地学基礎の絞り込み）。
- メインは**生徒テーブル（または生徒カードのグリッド）**。デスクトップではテーブル、狭い画面ではカードに切り替えるレスポンシブ設計を推奨。
- 各行（カード）に：生徒名、履修科目チップ、科目ごとの直近偏差値・前回比（↑緑/↓赤）、直近合計点、最終受験日。
- 行をクリック/タップで個人詳細へ遷移。

### 並べ替え・絞り込み

- ソート：名前順 / 直近偏差値順 / 前回比（伸び・落ち込み）順 / 最終受験日順。
- **要注目の生徒を見つけやすく**することを意識：前回比が大きく下がった生徒、長期間未受験の生徒を視覚的に目立たせる（バッジや色）。

### 補助ビュー（任意・あると望ましい）

- 科目を1つ選んだとき、全生徒の偏差値分布を簡易ヒストグラム/帯で見せる「クラス俯瞰」ミニカード。
- ただし**中心はあくまで一覧→個人詳細**。俯瞰ビューは補助に留め、画面を圧迫しないこと。

### 状態

- ローディング：行スケルトン。
- 空（担当生徒0/データ0）：導線つきの空表示。
- エラー：再試行ボタン。

---

## 6. 画面②：生徒個人詳細（成績推移）

既存の生徒向け成績推移画面とほぼ同じ。上部に「← 一覧へ戻る」を置く。構成は上から順に：

1. **科目切替タブ**
   - 履修科目が 2 つ以下：セグメントコントロール。
   - 3 つ以上：横スクロールのチップ。
   - 切替時は再通信せず、取得済みデータから該当科目を出すだけ（瞬時に切り替わる）。

2. **生徒名 + 科目ラベル**

3. **合計点の推移カード**
   - サマリー：直近テストの合計点・前回比・平均・偏差値。
   - 折れ線グラフ：**合計点（実線）/ 平均点（破線）/ 偏差値（右第2軸の薄い線）** の 3 系列を重ねる。左軸＝点数、右軸＝偏差値。
   - テスト件数が多いとき（14件超）：「直近12 / 直近30 / 全期間」の期間チップで範囲を絞れる。
   - グラフは 2 件以上で描画。1 件のときは「線が引けない」旨のメッセージ。
   - その下に、テストごとの記録（日付・テスト名・合計点・偏差値）をカード内スクロールの一覧。

4. **分野別の推移カード**（2 ビュー切替）
   - **「折れ線」ビュー**
     - 指標切替：得点率 / 平均得点率 / 偏差値。
     - 分野チップをタップして表示分野を選択（初期は未選択）。
     - 「強弱6」プリセット：直近月の上位3分野＋下位3分野をまとめて選択。「全て」「クリア」も用意。
     - 選択分野には固定 categorical パレットから安定した色を割り当て。
     - 選択中の分野は、月毎の得点率・偏差値を一覧でも確認できる。
   - **「得点傾向」ビュー**
     - 直近月の値で並べた「得意（上位）」と「苦手（下位）」を、前回比の増減つきの横棒で対比表示。
     - 表示件数：化学・生物は上位/下位 **5件ずつ**、基礎3科目（化学基礎・生物基礎・地学基礎）は上位下位が被らないよう **3件ずつ**。
     - このビューでは指標から **「平均得点率」を除外**（クラス全体の難易度を表す値で、個人の得意不得意の判定にそぐわないため）。

### 状態

- ローディング：スケルトン。
- エラー：再試行ボタン。
- 空：「データなし」表示（先生用では点数報告フォーム導線は不要。代わりに「一覧へ戻る」）。

---

## 7. グラフの描画仕様（重要）

グラフは外部チャートライブラリを使わず、**手書き SVG** で描く方針を踏襲する（軽量・WebView 負荷低減のため）。線は基底状態から可視で、アニメが止まっても内容が読めること。軸目盛りは値の範囲から自動スケール。

### 7-1. 合計点の推移

- 左軸＝点数、右軸＝偏差値の 2 軸。
- テスト件数が 24 を超えるときは個別ドットを省き、線だけで全件を 1 画面に収める。

### 7-2. 分野別の折れ線（★今回の修正ポイント）

**欠測月（`null`）の扱いを次のとおりにする：**

- データの無い月は **グラフから取り除き（スキップし）、存在する月だけを線で繋ぐ**。
- これにより **線が途切れることはない**。欠測で線を切ったり、空白を空けて分断したりしない。
- 補間（無い月の値を勝手に推定して埋める）はしない。あくまで「存在する月だけを順につないだ連続線」にする。
- 横軸は、選択中の分野について**実際に値が存在する月だけ**を等間隔で並べる（欠測月のための空きスペースは作らない）。
- 複数分野を同時表示する場合、各分野で値のある月が異なりうる。各分野の線は「その分野で値のある月」を順につなぐ。横軸ラベルは表示中の全分野で値が存在する月の和集合を等間隔に並べ、各線はその上で自分の値がある点だけを結ぶ（自分に値が無い月の位置は飛ばして次の値へ直接つなぐ）。

> 補足：従来仕様は「欠測が挟まると線を切る」だったが、本アプリでは上記のとおり**欠測月を詰めて連続線にする**よう変更する。

---

## 8. ビジュアル・トーン

- 教育系の落ち着いた信頼感のある配色。データが主役なので、背景は白〜淡グレー、テキストは高コントラスト。
- アクセントカラー1色（ブランドに合わせる）。前回比は **上昇＝緑、下降＝赤**、横ばい＝グレー。
- 分野の折れ線は **固定の categorical パレット**（8〜12色程度）で、同じ分野には常に同じ色を割り当て、再描画でも色が変わらないこと。
- 余白を十分に取り、一覧では情報密度を確保しつつ視線誘導を明確に。
- フォントは可読性重視。数値は等幅寄りで桁を揃える。

---

## 9. インタラクション/パフォーマンス指針

- 個人詳細は **1 生徒1リクエストで全科目取得**し、タブ切替で再通信しない。
- 一覧→詳細→一覧の往復で毎回再取得しないよう、取得済みデータはキャッシュ（SWR：直前データを即描画し、裏で再取得して差分があれば差し替え）。
- ヘッダーの**更新ボタン**で最新データを取り直せる（キャッシュ破棄）。
- 通信失敗時はモックで誤魔化さずエラー表示＋再試行。`NO_DATA` のときだけは空状態として素直に表示。

---

## 10. レスポンシブ

- **デスクトップ**：一覧はテーブル、詳細は2カラムも検討可（左にサマリー、右にグラフ）。
- **タブレット/スマホ**：一覧はカード、詳細は既存スマホ画面と同じ1カラム積み上げ。
- グラフは幅に応じて伸縮し、横軸ラベルは混雑時に間引く。

---

## 11. Claude Design への成果物リクエスト

- 上記3画面（トークン認証 / 生徒一覧 / 生徒個人詳細）の**高解像度モックアップ**。
- 各画面の**状態違い**（ローディング / 空 / エラー / 通常）。
- 主要コンポーネント（生徒行・カード、科目タブ、合計点グラフ、分野折れ線グラフ、得点傾向の横棒、分野チップ、前回比バッジ）の**コンポーネント定義**。
- カラーパレット・タイポグラフィ・余白のデザイントークン。
- レスポンシブのブレークポイント別レイアウト。
- **特に分野別折れ線は「欠測月を詰めて連続線にする」第7-2項の挙動**が一目でわかるモックを含めること。

---

## 12. 制約・前提（参考）

- 生徒と成績データの紐づけは名前の完全一致前提（同姓同名は想定外）。一覧でも生徒識別は名前ベース。
- 基礎科目のシートが未作成の場合、その科目はデータなし扱い。
- 先生は閲覧のみ。本アプリからのデータ編集・点数入力は想定しない。
